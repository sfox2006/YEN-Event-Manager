{
  "name": "yen-event-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check js/app.js && node --check js/api.js && node --check js/utils.js"
  },
  "engines": { "node": ">=20" }
}

